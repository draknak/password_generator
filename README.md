Simple command line password generator written in c.

Useage:
I use this in WSL.
Complie either with $make or $gcc main.c -o pswd_gen
run with $./pswd_gen <"len">
Can be used with or without the len parameter.